REPLIT, ATENÇÃO:

O problema está acontecendo somente no celular:
Quando o usuário clica no campo de digitar mensagem e o teclado do celular abre, o cabeçalho da conversa está sendo empurrado para cima e desaparecendo, e a tela inteira está rolando junto.

O correto é que o cabeçalho permaneça fixo no topo, sempre visível, sem nunca se mover.

Então faça o seguinte ajuste imediatamente:

✅ 1. Fixar o cabeçalho no topo
.chat-header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  height: 56px;
  background: #ffffff;
  border-bottom: 1px solid #dedede;
  display: flex;
  align-items: center;
  padding: 0 12px;
  z-index: 9999;
}

✅ 2. Fixar o campo de mensagem no rodapé (acima do teclado)
.chat-input-wrapper {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: #ffffff;
  border-top: 1px solid #dedede;
  padding: 8px;
  display: flex;
  gap: 10px;
  z-index: 9999;
}

✅ 3. Fazer apenas as mensagens rolarem
.chat-messages {
  margin-top: 56px; /* espaço do cabeçalho fixo */
  margin-bottom: 60px; /* espaço do input fixo */
  overflow-y: auto;
  height: calc(100dvh - 116px); /* altura real considerando teclado */
}

✅ 4. Corrigir o problema do 100vh no celular

Substituir:

height: 100vh;


Por:

height: 100dvh;


100dvh usa a altura real da tela mesmo quando o teclado está aberto, resolvendo o bug de empurrar o conteúdo.

📌 Explicando como deve ficar o layout final (REPLIT, siga isso):
┌───────────────────────────┐
│   .chat-header (FIXO)     │  ← não mexe nunca
├───────────────────────────┤
│                           │
│   .chat-messages (SCROLL) │  ← só esta parte rola
│                           │
├───────────────────────────┤
│   .chat-input-wrapper     │  ← sempre acima do teclado
└───────────────────────────┘
      Teclado do celular

✅ Resultado esperado:

O cabeçalho não some nunca.

O campo de digitar fica acima do teclado.

Somente a área das mensagens rola, independente de modelo de celular ou navegador.

Funciona para Android e iPhone.