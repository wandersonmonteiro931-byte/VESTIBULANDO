# Correção do build do AdminDashboard

Foi corrigido o fechamento do bloco responsivo de alunos em `AdminDashboard.tsx`.

O erro era:

`Expected ">" but found "className"`

O build `npm run build:pages` foi executado com sucesso após a correção.
